# docs/setup/

Step-by-step instructions written for the owner whenever an actor needs a human to do
something outside the repo (see the Human-in-the-loop protocol in
`docs/spec/constitution.md`). One file per task, named `<slug>.md`.
