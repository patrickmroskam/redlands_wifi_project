# Redlands Wifi Project — PO task backlog (process queue, FIFO)

> **Cycle 18 pruned this queue.** Seven tasks were retired to Done: every one of them
> was keyed to an issue that has since closed (#22, #25, #29, #30, #34) or to a gate
> that has since been satisfied. They were carrying pre-v1 state and made the live
> queue unreadable. The three standing tasks below survive unchanged in substance.

## Open

- [ ] (2026-09-18, cycle 4) Run the #17 takeover **tripwire** every cycle. — Resolve the apex and read its status: `curl -s -o /dev/null -w '%{http_code}' https://redlandswifiproject.com/`. **404 = still unclaimed, latent, do nothing.** **200, or a body that is not GitHub's "Site not found" page, = the name has been claimed by someone else** — fire a `high` notify immediately, regardless of the owner's deferral or any rung state. Closes instead if a `_github-pages-challenge-patrickmroskam` TXT resolves, or the apex `A` records leave 185.199.108-111.153. **Standing task, never closes.** **#17 now carries `needs-human` — this tripwire is EXEMPT from that parking.** It is a `curl`, not an issue read; run it every cycle regardless of the label. Readings: cycles 4–17 latent. **Cycle 18: latent, fifteenth consecutive.** **Cycle 19: latent, sixteenth consecutive** — 404 on GitHub's own "Site not found" page, apex `A` 185.199.108–111.153, no challenge TXT. **Cycle 20: latent, seventeenth consecutive** (same three readings). **Cycle 22: CLOSED.** NS is Cloudflare, with no apex `A`, no `www`, and curl `000`. #17 closure proposed. From now on the tripwire fires only if a record points at 185.199.108–111.153 or `*.github.io` without the challenge TXT; that is a `high` notify.

- [ ] (2026-09-19, cycle 5; widened cycle 6) **Reachability sweep — park anything an actor could reach but could not do.** The test is **reachability, not labelling**: for *every* open issue ask (1) is there a path by which this reaches the top of a tier, including a path through another actor stripping its current label, and (2) if it got there, could an actor actually do it? Repo settings, DNS, billing, account toggles, secrets and purchases all mean no. Park anything that answers yes-then-no, using the **narrowest durable label that does not disable the machinery scheduled to resolve it** — `blocked` when an issue waits on another issue, `needs-human` only when a human is the sole mover and no PO sweep is pending. **Standing, every cycle.** Cycle 18: swept all 5 open issues; set **empty**. **Cycle 19: swept the same 5 (#1, #17, #21, #63, #65); reachable-and-undoable set **empty**, no labels and no priorities changed.** **Cycle 20: swept the 4 left (#1, #17, #21, #63) after #65 closed; set **empty**, nothing changed.** (Cycle-19 note:) #65 stays `p3` — post-v1 by R10's own text, and the only workable item, so the tier has nothing to order against. #63 is the highest open tier (`p2`) but durably `needs-human`; #1 carries no priority label so no worker selects it.

- [ ] (2026-09-18, cycle 3) Never report "no workable issue" without the evidence. — Cycles 1–2 justified the disabled routine as an empty backlog; it was not empty. Done when: **standing** — every cycle that claims there is no agent work cites the `gh issue list --json number,labels,assignees` result that shows it, rather than inferring it from the routine being off. Cycle 18: the claim made this cycle is the opposite one (#65 *is* workable and *is* claimed), cited from the same listing. **Cycle 19: same claim, same citation** — #65 is workable, `p3`, and assigned; the stall is the wedged worker, not an empty backlog. **Cycle 20: the claim IS "no agent work"** — cited: 4 open, #1 `tracking`, #17/#21 `p3 blocked needs-human`, #63 `p2 needs-human`, none assigned.


- [x] (2026-09-23, cycle 20 → **DONE cycles 21–22: answered `mac`** — #73 filed, R9.4 committed, ingest-on-upload declined; the dev-team re-enable is deferred behind the apparent owner pause, see the cycle-22 task) **Resume-check ask `msg_18000c3c-4780-4cd7-9a11-002884b1db46` every cycle** (it carries no `waiting` label, so the task file's resume sweep will not find it on its own). — `oh-hai ask await --id msg_18000c3c-4780-4cd7-9a11-002884b1db46 --await-timeout 1000`. Never re-ask while it's open. On `both`/`mac`: flip R9.4's marker to ratified and file a `p3` issue to add the full-MAC invariant to `constitution.md`. On `both`/`upload`: file a `p3` issue for ingest-on-upload, with its owner-token step routed through the HITL protocol (`docs/setup/<slug>.md` + `waiting`). On either of those, re-enable `autonomy-dev-team-redlands-wifi-project`. On `neither`: strike both proposals, record the ruling, and leave the routine off. Done when it's answered and acted on.

- [x] (2026-09-23, cycle 19 → **folded into the cycle-20 ask**) **Carry the R9.4 full-MAC amendment until the owner rules.** — Proposed this cycle in the PRD as *awaiting owner ratification*, and put to the owner in the cycle-19 notify. It hardens shipped, owner-confirmed behaviour (#70) rather than adding scope, so it is a proposal and **not** an issue for dev-team. Done when: the owner ratifies (strike the marker, and file the one-line `constitution.md` change as a `p3` issue), **or** the owner declines (strike the marker and record the ruling). If it is still unruled at cycle 24, drop it and keep the invariant where it already lives — the ruleset comment and `flock.html` — rather than asking a third time.

- [ ] (2026-09-23, cycle 19) **Read the owner's channel, not just the hub — including live sessions.** — Widens the cycle-12 rule. This cycle found three owner instructions (19:40Z, 20:21Z, 22:07Z) and two merged PRs in a window the hub showed as **silent since 19:48Z**. Cycle 18 mis-read the same session as a concurrent scheduler replay. Done when: **standing** — before reporting owner inaction, check `git log origin/main`, merged PRs, and `list_sessions` for an owner-driven session in the window, and say which of those were checked. The hub remains the source of truth for **what this agent sent**; it is not the source of truth for what the owner did.

- [ ] (2026-09-24, cycle 22) **Re-enable the dev team for #73 once the owner pause is over.** — Cycle 22 found the PO routine and both No Mylar routines at `enabled:false`, and treated that as an owner pause. Done when: a PO run sees itself running on schedule again (the pause is over) and #73 is still open and unassigned → `update_scheduled_task autonomy-dev-team-redlands-wifi-project enabled=true`. Or the owner turns it on directly.
- [ ] (2026-09-24, cycle 22) **Check the ingest after the `.gpx` upload.** — The next scheduled run should publish +1,941 networks / +1,548 BT (dry run) and exit red on `wardrive_poi_0.gpx` (R4.10). Red with a published log is expected, not a `high`. Done when: the inbox no longer holds the `.gpx`. Do not re-ask for skip-`*.gpx` scope; the owner declined the question 2026-09-24T04:16Z.

## Done

- [x] (2026-09-23, cycle 19 → **DONE cycle 20, case (a)**) The #65 wedge. The app was relaunched, #65 shipped as PR #72 (`bb657ed`), and the dev-team disabled itself on the empty backlog. Original text kept for the record:
  > (2026-09-23, cycle 19) **The #65 wedge is settled as permanent — next cycle records only what cleared it.** — Cycle 18's three-way trigger fired **case (a)** this cycle: `local_7e45b5c4…` still `running` at the unchanged 15:16:41Z mark, PID 52097 alive 10 h 32 m, ten hourly fires skipped. Do **not** re-argue the diagnosis, re-measure the freeze, or re-litigate whether to unassign #65 / kill the process / flip the routine — all three were rejected with reasons that still hold (live process, shared `patrickmroskam` identity, slot pinned by the session not the flag). Done when cycle 20 records **one** of: (a) `lastRunAt` has moved past 2026-09-22T15:10:31Z → the app was relaunched; reconcile #65's claim, expect the ~3 min of `wt65` work to be gone and redone. (b) Still wedged **and** the owner has been active since the `high` notify → say so once, plainly, at `normal`; do not escalate a second time, because a second `high` on an optional post-v1 item is noise, not urgency. (c) The owner rules #65 deferred → drop it from the workable set and disable the hourly routine per the task-file rule.

- [x] (2026-09-22, cycle 18 → **FIRED cycle 19, case (a)**) Re-read the #65 worker's status before anything else next cycle. — **Case (a): still `running` at the unchanged 15:16:41Z mark**, PID 52097 alive 10 h 32 m, ten hourly fires skipped (16:10→01:10). The wedge is called **permanent** (the rung-18213 class), not slow, and #65 cannot move until the owner relaunches the app. Nothing was unassigned, killed or re-flipped, exactly as the trigger required. The trigger worked: it named the evidence in advance, so this cycle read a state instead of re-deciding a policy. Superseded by the cycle-19 task above. Original text kept for the record:
  > - [ ] (2026-09-22, cycle 18) **Re-read the #65 worker's status before anything else next cycle.** — Session `local_7e45b5c4…` is wedged: `running`, last activity 15:16:41Z, PID 52097 alive, holding the #65 claim and uncommitted R10 work in worktree `wt65` on `feat/65-network-kinds`. It pins the hourly slot, so 16:10–20:10Z all skip. Done when cycle 19 records one of: (a) **still `running` at the 15:16:41Z mark** → call the wedge permanent (the rung-18213 class), stop describing it as slow, and report that #65 cannot move until the owner relaunches; (b) **ended without progress** → the claim is orphaned, and only then consider releasing the assignee, citing the worktree evidence rather than the assignee; (c) **ended with a PR** → reconcile #65 and #1 normally. **Do not unassign, kill the process, or re-flip the routine on any of these paths without the evidence naming which case it is.**

- [x] (2026-09-22, cycle 18 → **SPENT cycle 19**) Do not re-raise the empty Flock map. — **The premise is gone.** The task assumed the ruleset would stay empty and told future cycles to record `flock: 0` and never ask. On 2026-09-22 the owner drove a live session, confirmed a camera, and said **"publish it"** at 22:07:25Z; `data/flock-rules.json` now carries `f0:82:c0:c1:fd:55` (#70, `8024c26`) and the live map serves **1 camera**. The task's own reopening condition — *"it reopens only if the owner supplies a log with a confirmed camera"* — is exactly what happened, which is the one way this retirement is a success rather than a drift. Nothing replaces it: the map is no longer empty, so there is nothing not to re-raise. Original text kept for the record:
  > - [ ] (2026-09-22, cycle 18) **Do not re-raise the empty Flock map.** — A 25,346-row batch this cycle produced `flock: 0` again. That is the design (`flock-rules.json` ships both lists empty so a guess cannot mark a resident's AP) and it is **parked by owner ruling 2026-09-21**. Standing: record the reading, never file it, never re-ask. It reopens only if the owner supplies a log with a confirmed camera.

- [x] (2026-09-18, cycle 4 → retired cycle 18) File the owner-approved git history rewrite as its own issue. — **Satisfied.** #63 ("Privacy: rewrite the 48 pre-cutover wardrive logs out of public git history, owner-authorised, human-run") is that issue, open at `p2` + `needs-human`. The constitution's line 28 also records that the rewrite is "its own issue and its own review".
- [x] (2026-09-18, cycle 4 → retired cycle 18) Re-run the objective #30 gate every cycle (four checks). — **All four are now positive and #30 is closed** (shipped in PR #43): `patrickmroskam/redlands_wifi_inbox` exists and is PRIVATE, `INBOX_TOKEN` authenticates (the `Check out the private inbox` step succeeds), and `INGEST_SCHEDULE` is set to `on` as a repository variable. The gate it guarded — R5 — is met. Nothing left to re-run.
- [x] (2026-09-18, cycle 1 → retired cycle 18) Aging sweep on #22's closure proposal. — **Mooted:** #22 is closed, and the private-inbox cutover it proposed (option C) shipped.
- [x] (2026-09-18, cycle 1 → retired cycle 18) Split #17 into the p1 takeover mitigation and the out-of-v1 domain switch. — **Mooted:** the owner ruled the custom domain post-v1, #17 sits at `p3` + `blocked` + `needs-human`, and the exposure is watched by the standing tripwire instead. A split would add an issue to carry a risk that one `curl` already covers.
- [x] (2026-09-18, cycle 2 → retired cycle 18) Verify #30 amends the constitution invariant when it lands. — **Verified this cycle.** `docs/spec/constitution.md:28` now reads "Never push the raw wardrive logs anywhere **public** … They go only to the private inbox repo `patrickmroskam/redlands_wifi_inbox`", and line 24 and the `ingest_dir` key at line 61 both name the private repo. No deadlock occurred.
- [x] (2026-09-18, cycle 3 → retired cycle 18) Carry the protocol step 4 amendment proposal until the owner rules. — **Dropped, exactly as the task instructed** ("if it is still unruled once both owner gates clear, drop it"). Both gates cleared: #30 shipped and the #17 question was ruled post-v1. The issues it was written to unpark (#25, #29, #34) have all shipped, so the problem it described no longer exists.
- [x] (2026-09-18, cycle 4 → retired cycle 18) Re-raise #17 once at cycle 9 if still unmitigated. — **Mooted:** #17 is parked `needs-human` by owner ruling and the standing tripwire covers the exposure. The one-shot reminder would now be repetition, not new information.


- [x] (2026-09-18, cycle 1 → **DONE cycle 10**) Split #17 into the takeover mitigation and the out-of-v1 domain switch. — **Cleared at cycle 10, six cycles after it was queued, five of those on a rationale that was never true.** Executed by the zero-delta route: **#17 retitled** to *"Security: close the redlandswifiproject.com takeover window (verify the domain, or drop the DNS records)"*, with an **additive banner** (`issuecomment-5748460508`) marking which paragraphs of the original body are now out of scope and the original text preserved verbatim. **No second issue filed** — the domain *switch* is Out of Scope (v1) in the ratified PRD and the owner answered `not-yet`, so it is now tracked by no issue at all, deliberately. **Priority left at `p2`, not raised to p1**: `needs-human` makes the tier label inert on this issue, and raising it would be a silent re-escalation against an owner decision whose rung is retired. Original text kept for the record:
  > - [ ] (2026-09-18, cycle 1) Split #17 into the p1 takeover mitigation and the out-of-v1 domain switch. — Done when: either a separate issue exists covering only "verify the domain on the GitHub account **or** remove the apex `A` / `www` `CNAME` records", with #17 retitled to the switch alone; **or** the exposure is gone (a `_github-pages-challenge-patrickmroskam` TXT resolves, or the apex `A` records no longer point at 185.199.108-111.153) and #17 is back at p3. **Cycle 9 correction — this was never budget-blocked.** Cycles 2–8 all deferred it as *"§4e needs ≥ 1 realized close"*. True of the **original** route (file a second issue); **not** true of the route cycle 4 actually chose — **retitling #17 to the mitigation alone files nothing, so net issue delta is 0** and §4e (which budgets *filing*) never applied. The deferral survived on an expired rationale. It matters now because the #22 sweep **will not** produce a close (rewritten above), so a cycle-10 run re-reading "blocked on budget" would defer it a seventh time for a reason that was never true. **It is unblocked — do it at cycle 10**, with the full rebuild. **Cycle 4 narrows the goal:** the owner's `not-yet` confirms the ratified PRD's Out-of-Scope line, so the domain *switch* needs no issue in v1 at all — the split can be satisfied by retitling #17 to the mitigation alone, at zero net issue delta, rather than by opening a second issue. Deferred to the same cycle regardless, to keep the retitle in one change with the sweep.

- [x] (2026-09-18, cycle 2 → retired cycle 4) Hold the #17 escalation rung. — **Retired unfired.** The rung's condition was "exposure still live **and** the ask still unanswered at cycle 4"; the ask came back `not-yet`, so the condition was never met. Cycle 4 re-aimed the escalation at #30 instead, on its own merits. Original text kept for the record:
  > - [ ] (2026-09-18, cycle 2) Watch #17's ask `msg_e812f339-e53b-46d4-bb67-ce8e5f6edced` and hold the escalation rung. — **Do not re-ask while it is open.** The gate is the DNS, not the reply: re-resolve the challenge TXT and the apex `A` records every cycle. Done when: either the exposure is closed (TXT resolves → drop #17 to p3 and retitle to the switch alone; or apex `A` leaves the GitHub range → close #17), or — if the exposure is still live *and* the ask still unanswered at **cycle 4 (~2026-09-18T19:38Z ≈ 12:38 PT)**, a full waking day with the ask in front of the owner — that cycle sends its notify at priority `high`. Cycle 3 (~06:38 PT) sends nothing new. **The rung was decided at cycle 2; later cycles execute it, they do not re-argue it.** Cycle 3: executed as written — gate re-resolved (still exposed), ask re-checked with `ask await` (still open), no re-ask, one `low` notify. **Cycle 4 fires the `high` rung, and keeps that message about the takeover window alone.**

- [x] (2026-09-18, cycle 3) Re-run both objective gates and hold the committed rung. — #30: repo / `ingest` environment / secrets all still absent (and variables 0). #17: challenge TXT still absent, apex `A` still in the Pages range, apex still 404, `pages.cname` still null; ask `msg_e812f339` re-checked → still open, not re-asked. One `low` notify sent; escalation left for cycle 4 as committed.

- [x] (2026-09-18, cycle 3) Correct the record on why the dev-team routine is off. — It is the constitution's step 4, not an empty backlog. Fixed in the state digest and the readiness report, logged as a reversal in the decision log, and turned into the standing evidence rule above.

- [x] (2026-09-18, cycle 2) Send the #17 ask that had never been sent. — Cycle 1 diagnosed the deadlock; cycle 2 cleared it: wrote `docs/setup/domain-takeover.md` (`f7368e0`), sent ask `msg_e812f339-…`, recorded it on #17, resume key `DONE domain-takeover`.

- [x] (2026-09-18, cycle 2) Chase #21 (p3, owner-only GitHub security settings). — Closed by recording on the issue that its steps have never reached the owner through the durable channel and that it is deliberately deferred behind the p1/p2 owner queue, with the unblock condition named. Rejected sending an ask of its own: a third, p3 ask competing with a live p1 security item lowers the odds of both.
- [x] (2026-09-18, cycle 1) Bootstrap the continuity artifacts (digest, decision log, PO tasks, objection ledger).
- [x] (2026-09-18, cycle 1) Clear `needs-triage` from #22, as the 2026-09-17 09:09Z comment requested.

- [ ] (2026-09-20, cycle 12) **Check the #22 window at cycle 13–14 — and this time the verdict IS eligible, so read the objection count, not the class.** — New proposal `addressed-in-#43` posted 2026-09-20T19:40Z; window shuts **2026-09-22T19:40Z**. Unlike every prior cycle, the verdict class now passes (`addressed-in-#NNN` with #43 **merged**), so the only remaining gate is a **post-proposal** `closure-objection:` — one posted *after* 19:40Z. **Match on structure, not mention:** the thread now contains the PO's own prose quoting the convention plus the superseded 16:05Z objection; neither is a post-proposal objection. Done when: either dev-team concurs and closes (**the PO's first realized close**), or a genuine post-19:40Z objection is recorded in the ledger and the verdict is re-worked. Do **not** self-close it as maker.
- [x] (2026-09-20, cycle 12) **File the git-history-rewrite issue in the FIRST cycle that realizes a close — this is a committed trigger, not another deferral.** — Ninth cycle deferred. Rationale re-tested at cycle 12 against the route actually available (not copied): §4e budgets filing against **realized** closes and cycle 12 realized 0, while the backlog rose 8 → 11 on dev-team inflow. Visibility is preserved meanwhile by dev-team's #22 carve-out and the standing entry in `docs/spec/readiness.md`. **Trigger:** the moment any close lands (most plausibly #22 at cycle 13–14), file it. Body must carry the known caveats: forks/clones already hold the objects, `data/networks.json` is unaffected, and **no agent runs the purge unattended** — the owner re-confirms in-session. Done when: the issue exists and this line carries its number. **FIRED AND DONE at cycle 13 (2026-09-21T01:44Z) — the issue is [#63](https://github.com/patrickmroskam/redlands_wifi_project/issues/63).** The trigger worked exactly as written and predicted the cycle: #22 closed 2026-09-20T20:05:34Z, and seven closes landed in the cycle-13 window, so the §4e budget was ample (+1 filed against 7 realized). Filed `p2` `needs-human`, post-v1, explicitly **not** gating the DoD, and added to #1 under a separate post-v1 heading. Body carries the caveats and two more the old text did not have: it is grounded in the object store (**48** files, **47 blobs / 4.0 MB**, exactly **2** commits `2aa60b0`/`cbb849b`, **0 forks**) rather than asserted, and the clone caveat now carries its number *with its limits* — 560 clones / 168 uniques in 14 days, reported as **consistent with this project's own Actions checkouts and not evidence of third-party copies**, because GitHub cannot attribute clones and a bare number would have read as 168 strangers. Also added: force-pushed objects stay SHA-reachable on github.com until GitHub Support GCs them. **Lesson, worth keeping:** a deferral survived nine cycles on a rationale that was re-copied rather than re-tested; what finally moved it was attaching a *committed trigger with an objective condition* instead of another cycle estimate.
- [ ] (2026-09-20, cycle 12) **Before concluding "no owner movement", check repo/API state — not just the OH HAI hub.** — Standing. Eight consecutive cycles reported zero owner movement from the hub; the owner created `redlands_wifi_inbox` (16:08:22Z) and `INBOX_TOKEN` (16:16:57Z) **without replying to anything**, and a hub-only read would have reported a ninth silent cycle while the blocker was being cleared. Done when: **standing** — every cycle that reports owner inaction cites an objective state check of the blocking item, not only the absence of a reply. This is the fourth instrument defect in four cycles and they share one shape: **the check drifted from the thing it was meant to check.**
- [ ] (2026-09-20, cycle 12) **Re-measure the BLE/BT latency claim when the first post-cutover log is ingested.** — Cycle 12 replayed all 48 historical logs from `0c10f06^`: **40,228 rows, 100% `Type=WIFI`**, zero `BT`, zero `BLE`. That is what keeps **#50** and **#42** at p3, and it is also why R9's Bluetooth map currently has no data at all. The rig is an ESP32 Marauder and **can** emit BLE rows if the owner runs a BLE scan. Done when: the first log ingested after #48 is fixed has had its `Type` column tallied, and #50/#42 are re-tiered on that evidence (raise to p2 the moment a real `BT` or `BLE` row appears — #50 is a live privacy loss at that point, not a latent one).
- [ ] (2026-09-20, cycle 12) **`INGEST_SCHEDULE=on` is bot work — do not route it to the owner as a second step.** — The `gh` token carries `repo`, so `gh variable set INGEST_SCHEDULE --body on` is an agent action. It must be a **repository** variable (the workflow's job-level `if` is evaluated before the `ingest` environment resolves, so an environment variable reads as empty and R5 stays silently broken). **Ordering is load-bearing:** it must not be set until `INBOX_TOKEN` authenticates, or the owner gets a failed job in their inbox every morning. Done when: #48 is resolved, a manual `workflow_dispatch` run succeeds end to end, and *then* the variable is set. The owner's remaining v1 work is **one** action, and this task is why.
