# Redlands Wifi Project — PO task backlog (process queue, FIFO)

## Open

- [ ] (2026-09-18, cycle 4) Run the #17 takeover **tripwire** every cycle. — Resolve the apex and read its status: `curl -s -o /dev/null -w '%{http_code}' https://redlandswifiproject.com/`. **404 = still unclaimed, latent, do nothing.** **200, or a body that is not GitHub's "Site not found" page, = the name has been claimed by someone else** — fire a `high` notify immediately, regardless of the owner's deferral or any rung state. Closes instead if a `_github-pages-challenge-patrickmroskam` TXT resolves, or the apex `A` records leave 185.199.108-111.153. **Standing task, never closes.** Cycle 4: 404, latent. **Cycle 5: 404, latent. Cycle 6: 404, and the body is GitHub's own "Site not found · GitHub Pages" page — latent. Cycle 7 (13:38Z): 404, same GitHub page, apex `A` still 185.199.108/109/110/111.153, challenge TXT still absent — latent. Cycle 8 (19:38Z): 404, same GitHub page, apex `A` unchanged, challenge TXT still absent, `pages.cname` still `null` — latent. Cycle 9 (01:39Z): 404, same GitHub page, apex `A` still 185.199.108/109/110/111.153, challenge TXT still absent, `www` CNAME still `patrickmroskam.github.io.`, `pages.cname` still `null` — latent, sixth consecutive run unchanged. Cycle 10 (07:41Z): 404, same GitHub page, apex `A` still 185.199.108/109/110/111.153, challenge TXT still absent, `www` CNAME still `patrickmroskam.github.io.`, `pages.cname` still `null` — latent, SEVENTH consecutive run unchanged. Cycle 11 (13:43Z): 404, same GitHub page, apex `A` still 185.199.108/109/110/111.153, challenge TXT still absent, `www` CNAME still `patrickmroskam.github.io.`, `pages.cname` still `null` — latent, EIGHTH consecutive run unchanged.** **#17 now carries `needs-human` — this tripwire is EXEMPT from that parking.** It is a `curl`, not an issue read; run it every cycle regardless of the label, the `not-yet`, or any rung state. **2026-09-20 OWNER RULING: the custom domain is deferred to post-v1 and #17 is now `p3` — this tripwire is EXEMPT from that ruling too.** Deferring the migration is not a willingness to lose the name; a claimed apex still fires `high` immediately. **Do not re-raise #17's priority and do not re-ask about the domain — the owner has now ruled twice (`not-yet`, then this).**
- [ ] (2026-09-18, cycle 4) Re-raise #17 **once**, at cycle 9 (~2026-09-20T01:38Z), if still unmitigated. — A single dated reminder against a `not-yet`, not a recurring one. Done when: raised once and logged, or the tripwire's close conditions hit first. **Do not re-ask, and do not escalate #17 on any other timer.** Cycles 5, 6, 7 and 8: not due. **✅ CYCLE 9 FIRED IT AND THE RUNG IS SPENT** — the single dated reminder was posted as a comment on #17 at 2026-09-20T01:41Z: tripwire re-run and latent, both ~5-minute exits from `docs/setup/domain-takeover.md` restated, and an explicit statement that **neither reverses the owner's `not-yet`** (neither serves the site on the custom domain; the switch is out of v1 scope per the ratified PRD). No re-ask sent — `msg_e812f339` is answered. **There is no further timer on #17.** Only the automated tripwire remains. **Cycles 10 and 11: correctly did not re-fire** — the rung is spent and no second reminder was sent. A cycle that feels the urge to remind again should stop: the owner has been told seven times, the last time with both 5-minute exits spelled out. Repetition is not new information. **Also exempt from #17's `needs-human` parking** — the label stops the sweep re-reasoning about #17, not this committed one-shot reminder.
- [ ] (2026-09-18, cycle 4) File the owner-approved **git history rewrite** as its own issue. — Authority exists: the owner replied "Rewrite history", `private-inbox.md`'s exact opt-in phrase. Recorded as comments on #30 and #22 this cycle; blocked on §4e's net-issue-delta budget (needs ≥ 1 realized close). **Cycle 5 correction:** the original "earliest cycle 5" was written before the #22 sweep was renumbered to **cycle 10**, and the budget comes from that sweep's close — so the earliest this can be filed is **cycle 10**, not cycle 5. **Cycle 9 supersedes that: the #22 sweep will NOT close anything** (see the rewritten sweep task above), so the budget it was waiting on never arrives. This issue **is** a new issue (+1 net delta), so unlike the #17 retitle it genuinely needs §4e budget — its unblock is now **any realized close**, with no cycle attached. Do not file it at cycle 10 on the strength of the old schedule. Cycle 5 realized zero closures and therefore filed nothing (§4e: "if you realized no closures this run, file no gap issues"). **Cycle 10 confirmed the supersession and filed nothing**: the #22 sweep produced no close, as cycle 9 predicted, so the budget never arrived. **The PO has realized zero closures in all 10 cycles**, so this has never once been fileable. It is not at risk of being lost — the authority and the scope are recorded here, in the decision log, and as comments on #30 and #22 — so deferring is safe, not neglectful. **Cycle 11 re-derived §4e from the skill source** (not from the previous cycle's text), as the cycle-10 rule requires of anything deferred 3+ cycles: *"The budget is only issues actually closed this run… If you realized no closures this run, file no gap issues."* Cycle 11 realized zero closes, so the block is **still genuinely true** — and the re-derivation surfaced a **second, independent** reason not to force it: §4e budgets *release-criteria* gaps, and history retention sits under the ratified PRD's "Known consideration (**not a requirement**)", so this is not a release-criteria gap at all. Both reasons must lapse, not just the budget. **Unblock: any realized close, no cycle attached.** The moment the owner clears #30, the closes that follow create the budget. **Must not ride along inside #30's cutover** — it is destructive and irreversible on a public repo, and anyone who already cloned keeps their copy, so it needs its own issue and its own review.
- [ ] (2026-09-18, cycle 4) Re-run the objective #30 gate every cycle — now **four** checks, not three. — `gh repo view patrickmroskam/redlands_wifi_inbox` (and the `-` spelling), an `ingest` entry in `repos/…/environments`, `gh secret list` non-empty (`INBOX_TOKEN`), and `gh variable list` non-empty (`INGEST_SCHEDULE`). The fourth is not optional bookkeeping: `INGEST_SCHEDULE` is the gate that makes every scheduled ingest run a no-op, so it is also R5's blocker. **Supersedes the cycle-1 three-check task. Standing, never closes.** Cycle 4: all four negative, logged. **Cycle 5: all four negative again, logged. Cycle 6: all four negative a third time, logged. Cycle 7: all four negative a FOURTH time, logged. Cycle 8: all four negative a FIFTH time, logged; cycle 9 (01:39Z): all four negative a SIXTH time, logged; cycle 10 (07:41Z): all four negative a SEVENTH time, logged; cycle 11 (13:43Z): all four negative an EIGHTH time, logged** (both repo spellings, `environments` `total_count: 1`, `secrets` 0, `variables` 0; `ask await` on `msg_92a6bc9c` re-read unchanged). Note the asymmetry with #17: **#30 must keep `waiting` and must NOT be given `needs-human`** — the task file's resume check enumerates open issues labelled `waiting`, so parking #30 would switch off the very check that resumes the project.

- [ ] (2026-09-18, cycle 1 · **rewritten cycle 9**) Aging sweep on #22's closure proposal — **it must NOT self-close.** — **Cycle 9 found the precondition fails, and rewrote this task before cycle 10 could execute the old version.** The 48 h window shut 2026-09-20T01:42:32Z with **0** post-proposal objections, so on the aging test alone #22 was due to self-close. It is **not auto-close eligible**: `closure-handshake.md` (the declared canonical contract) gates the verdict class *before* the check-and-act — `addressed-in-#NNN` means the work shipped in a **merged PR** #NNN, eligible "**Yes if PR #NNN is merged**", and Path 2 repeats it as "with the named issue/PR **confirmed closed-completed on re-check**". Verified live at cycle 9: `GET /repos/.../pulls/30` → **404** (#30 is an issue, not a PR), `gh issue view 30` → `state=OPEN` / `p2,waiting`, and no merged PR shipped option C (#37 = the setup doc; #26 = the privacy-page disclosure that the logs are public, the opposite of a fix). **Done when:** each cycle records that the sweep ran and routed #22 to the human digest as *"awaiting human closure decision"* — or, once option C ships, the verdict is **re-pointed at that merged PR number** (not #30) and only then may self-close. **Keep `po-closure-proposed`** — the proposal is genuinely open (neither concurred nor objected) and the label is what keeps #22 in dev-team's step-0 queue, the handshake's real checker. **Do not** remove the label, withdraw the verdict, re-propose under `obsolete`, or record this in `objections.md` (an ineligible verdict is not a veto, and logging it as one would wrongly bar a legitimate re-proposal later). **#22's verifiable path is now dev-team step-0 adjudication on resume — NOT this sweep** (§4h relies on that path existing). The old text read *"#22 is either self-closed with verdict `addressed-in-#30`"*, which drops the eligibility gate and would have deleted the only open record of a **live** privacy exposure — raw GPS drive trails still in public git history — at the moment the owner is being asked to fix it. **Standing, every cycle until option C ships.**

  **✅ CYCLE 10 RAN IT AS REWRITTEN — the trap did not fire.** This was the cycle the old text would have self-closed #22. Re-verified live at 07:41Z rather than inherited from cycle 9: `GET /repos/.../pulls/30` → **404**; `gh issue view 30` → `state=OPEN`, `p2,waiting`; all **15** merged PRs scanned, none ships option C (#37 = owner setup doc, #26 = the privacy-page disclosure). Verdict **ineligible**; #22 **routed to the human digest as _awaiting human closure decision_** (`issuecomment-5748458979`). `po-closure-proposed` kept, verdict not withdrawn, nothing written to `objections.md`. Re-verify live each cycle — do not inherit this result, because the one thing that changes it (option C shipping) is exactly what the project is waiting for.

  **✅ CYCLE 11 RE-VERIFIED IT LIVE — still ineligible.** `GET /repos/…/pulls/30` → **404**; `gh issue view 30` → `state=OPEN`, `p2,waiting`; all **15** merged PRs re-scanned, none ships option C. `po-closure-proposed` kept, verdict not withdrawn, nothing written to `objections.md`. **No new issue comment posted** — cycle 10's `issuecomment-5748458979` already states exactly this, and a verbatim repeat every 6 h on a public issue is noise; "records that the sweep ran" is satisfied by the decision log and digest entries, which are durable and in-repo. **Future cycles: keep recording in-repo, and only comment on #22 again when the result actually changes.** Cycle 11 also found the objection count itself was being mis-measured — see the substring-match task below.

- [ ] (2026-09-20, cycle 10) **A deferral's rationale expires — re-test it, don't re-copy it.** — The #17 split sat queued for six cycles. Cycles 2–8 each re-copied *"blocked: §4e needs ≥ 1 realized close"* from the previous cycle's text without re-testing it against the route actually chosen; cycle 9 found it had been false since cycle 4 (§4e budgets *filing*, and a retitle files nothing). The failure mode is not a wrong rule — it is a **stale rationale surviving by inheritance**, and it is the mirror image of the cycle-9 compression trap: that one dropped a precondition, this one kept one that had already lapsed. **Standing, every cycle:** for each task still open here, ask *"is the reason this is deferred still true today, and is it true of the route I would actually take?"* — and if a task has been deferred three or more cycles on the same stated reason, re-derive that reason from source before deferring it a fourth time. Cycle 10 applied this and cleared the #17 split; the history-rewrite task was re-tested the same way and its block (needs any realized close) **is** still true, so it stays deferred — the test is meant to catch both outcomes, not to license clearing the queue.

- [ ] (2026-09-20, cycle 11) **Match on structure, not on mention — the PO's own prose is now in the corpus it greps.** — Cycle 11 ran two mention-based checks and **both returned false positives**. (1) The post-proposal `closure-objection:` count on #22 came back **2**; both hits were the PO's *own* comments — cycle 6 explaining that dev-team *"posts a `closure-objection:` and strips `po-closure-proposed`"*, and cycle 9's own results table containing the literal row `| post-proposal closure-objection: count | 0 |`. True count: **0**. A run trusting that number would have concluded an objection existed and, on the handshake's disagree branch, stripped `po-closure-proposed` and written a veto into `objections.md` — exactly what the rewritten #22 sweep forbids, and it would have barred a legitimate re-proposal once option C ships. (2) The halt-or-re-ask sweep flagged **#29** on *"wait for the running pan to finish before restoring the fence"* — a Leaflet `moveend` code suggestion. **The mechanism is structural and gets worse with age:** every cycle the PO writes more prose *about* `closure-objection:`, `waiting`, `needs-human` and `po-closure-proposed` onto the very issues it later searches, so the false-positive rate of any mention-based check rises monotonically. This is a distinct failure class from cycle 9's dropped precondition and cycle 10's expired rationale — here the *instrument* is degrading, not the reasoning. **Standing, every cycle:** any check whose result gates an action must (a) match on **structure** — a real objection is a comment whose body *begins* with `closure-objection:`, not one containing it — and (b) **exclude comments authored by the PO's own runs** from counts of other actors' signals. When a mention-based grep is used as a cheap pre-filter, every hit must be opened and read before it is counted. **A count is not evidence until you have read what it counted.**

- [ ] (2026-09-20, cycle 9) **When a task here restates a skill rule, carry its PRECONDITIONS, not just its action.** — The #22 trap above was not a misreading of the contract; it was a **compression** of it. The handshake's own check-and-act snippet tests only `state` / post-proposal objection / `launch-blocker` — it omits the named-target check because eligibility is decided in the prose above it — and this file had compressed the rule to "self-close with verdict `addressed-in-#30`". A run following either text top-down closes the issue. This file is the most-read compression in the system, so a dropped precondition here is load-bearing. **Standing, every cycle:** when writing or re-reading a task that restates skill behaviour, check it against the skill text for a precondition the summary dropped.
- [ ] (2026-09-18, cycle 1) Re-run the objective #30 resume gate every cycle. — Done when: each cycle's decision log records the result of all three checks — `gh repo view patrickmroskam/redlands_wifi_inbox`, an `ingest` entry in `repos/…/environments`, and `actions/secrets total_count > 0` — rather than an interpretation of the ask reply. **Standing task, never closes.** Cycle 2: run, all three negative, logged. Cycle 3: re-run 13:39Z, all three still negative (plus `actions/variables` → 0, so `INGEST_SCHEDULE` is also still unset), logged. **Cycle 4: SUPERSEDED** by the four-check version at the top of this list, which promotes `INGEST_SCHEDULE` from a parenthetical to a first-class check because it is also R5's blocker.
- [ ] (2026-09-18, cycle 2) Verify #30 amends the constitution invariant when it lands. — Done when: the commit that moves the raw-log flow also rewrites "Never push the raw wardrive logs anywhere except `ingest/` in this repo" to name the private inbox. The authority already exists (ask `msg_92a6bc9c` states the reply ratifies the rule change; the owner replied `yes`) and #30's body scopes it, so this is a check, not new work. If a dev-team run instead *stops* on that invariant, that is a real deadlock — raise it immediately. Cycle 3: not yet reachable — the routine is disabled, so no dev-team run can hit it. **Cycle 7 strengthens the authority trail:** #30's body now carries a banner stating the constitution change is pre-ratified (the ask said the reply ratifies it; the reply was `yes`), so a run that reaches this step has the evidence in front of it and should not stop on the current invariant wording. Cycles 4–8: still unreachable, and now better grounded — the owner's reply to `msg_92a6bc9c` was `yes`, so the ratification the task relies on is confirmed rather than assumed.

- [x] (2026-09-18, cycle 3 → **RULED AND APPLIED 2026-09-20**) Carry the **protocol step 4 amendment** proposal until the owner rules on it. — **The owner ruled: amend and re-enable.** Step 4 now skips blocked issues and continues; the routine disables only when no unblocked p0–p3 issue remains. `constitution.md` amended and `autonomy-dev-team-redlands-wifi-project` set `enabled: true` in the same change. #25/#29/#34 are unparked after ~70 h. Original text kept for the record:

  > - [ ] (2026-09-18, cycle 3) Carry the **protocol step 4 amendment** proposal until the owner rules on it. — The constitution halts the whole dev-team routine for any live `waiting` issue, so #25, #29 and #34 (all p3, unassigned, unblocked, none dependent on #30 or #17) have been parked since 2026-09-17T15:05Z. Proposed: step 4 becomes *"skip the blocked issue and continue with the next unblocked one; disable the routine only when no unblocked p0–p3 issue remains."* **Not the PO's to apply** — it is a hard invariant, and re-enabling a routine changes the owner's machine and compute. Sent as a one-line recommendation in cycle 3's `low` notify, **not** as an ask (queue contention with the live p1 ask `msg_e812f339`). Done when: the owner rules (apply the edit and re-enable in that cycle, and log the sign-off), **or** `DONE private-inbox` arrives and moots it (the routine resumes on its own). If it is still unruled once both owner gates clear, drop it — the problem disappears with them.

- [ ] (2026-09-19, cycle 5) **Check every unlabelled issue in a workable tier for owner-only work — before dev-team resumes.** — Cycle 5 found #21 sitting as the oldest `p3`, unassigned and unlabelled, whose body says an agent may not do it; a resumed dev-team run would have selected it first and immediately re-disabled itself on protocol step 4, re-parking #25/#29/#34 and spending a second owner interruption. Fixed label-only (`blocked` + `needs-human`). **Standing, every cycle:** for each open issue with no `blocked`/`waiting`/`needs-human`/`po-closure-proposed` label, ask whether an actor can actually do it — repo settings, DNS, billing, account toggles, secrets and purchases all mean no. Park those before they reach the top of a tier. Cycle 5: swept all 8 open issues; #21 was the only one, and the resume order is now #30 → #25 → #29 → #34.

  **Cycle 6 widens this task — the cycle-5 scope was the wrong test.** "No exclusion label" assumed every exclusion label is durable parking. `po-closure-proposed` is not: the dev-team's step-0 checker duty removes it on a **disagree** verdict, and `blocked`/`waiting` can be removed by the merge-time unblock step. The test is therefore **reachability, not labelling** — for *every* open issue ask: (1) is there a path by which this reaches the top of a tier, including a path that runs through another actor stripping its current label? and (2) if it got there, could an actor actually do it? Park anything that answers yes-then-no, using the **narrowest durable label that does not disable the machinery scheduled to resolve it** — `blocked` when an issue is waiting on another issue, `needs-human` only when a human is the sole mover and no PO sweep is pending on it. Cycle 6: found #22 by this test (unlabelled-scope was blind to it), parked it `blocked`; reachable-and-undoable set is now empty.

  **Cycle 7 adds a THIRD question — staleness.** #30 passes both existing tests (it reaches the top of a tier, and an actor *can* do it once the setup exists) and was still a trap: two instructions in its body predate the owner's answer — "write `docs/setup/<slug>.md` and send the ask first" (done) and "**Not decided — ask the owner, don't assume:** whether to rewrite history" (decided: "Rewrite history"). Read top-down and literally, either sends a resuming run back into protocol step 4. So the sweep now asks, for every open issue: **(1) reachable? (2) doable? (3) does its own text instruct an actor to halt or re-ask about something already resolved?** Question 3 bites hardest on whichever issue is **next in line**, whose text is oldest relative to the decisions taken since. Remedy is **additive** — a status banner marking the resolved items with their evidence, original text preserved verbatim — never a rewrite of the owner's framing. Cycle 7: swept all 8; reachable-and-undoable **empty** (third cycle), reachable-and-stale **{#30}**, banner added. **Cycle 8: swept all 8; both sets empty.** #25/#29/#34 re-read in full — pure repo work, no halt-or-re-ask instruction, no owner-decision language; #30's banner verified present with the original body preserved verbatim below it. A clean sweep is the honest result, not a licence to go looking for a fourth trap. **Cycle 9: swept all 8 again; both sets empty (fifth consecutive clean).** Zero issue activity since cycle 8's watermark (`search/issues?...updated:>=2026-09-19T19:38:00Z` → `total_count: 0`), so inputs were re-read rather than assumed. #1 re-read and explicitly cleared: its R5 line says set `INGEST_SCHEDULE=on` **after the private-inbox cutover** — correctly ordered, not a trap — and it carries no priority label, so it never enters a tier search. **The cycle-9 find was on the PO's own closure path, not in an issue body** — as the last four have been. Do not read five clean issue-body sweeps as a reason to lower the bar, nor as pressure to manufacture a landmine in the issues. Confidence on #30 recorded as **medium**, below #21/#22, because the resolution already existed in comments — logged deliberately, since two consecutive landmine cycles create pressure to manufacture a third.

  **Cycle 10: swept all 8 again; both sets empty (SIXTH consecutive clean)** — and this time the corpus was *provably* unchanged rather than re-derived: every open issue's `updatedAt` predates the cycle-10 watermark, with only cycle 9's own comments on #17 and #22 falling between, and `search/issues?...updated:>=2026-09-20T01:45:00Z` → `total_count: 0`. #25/#29/#34 and #1 re-read and pattern-checked for halt-or-re-ask language → none. Five of the last six finds were on the PO's own path, not in an issue body. **Six clean sweeps is the honest result. Do not lower the bar, and do not manufacture a seventh landmine to justify the cycle.**

  **Cycle 11: swept all 8 again; both sets empty (SEVENTH consecutive clean)**, again against a *provably* unchanged corpus — every open issue's `updatedAt` predates the cycle-11 watermark and `search/issues?…updated:>=2026-09-20T07:45:00Z` → `total_count: 0`. #25/#29/#34 and #1 re-read and pattern-checked. The pattern check flagged **#29**, and it was a **false positive** — *"wait for the running pan to finish before restoring the fence"* is a Leaflet `moveend` implementation suggestion, not an owner halt. That near-miss is the cycle-11 find, generalised in the task below.

- [ ] (2026-09-19, cycle 5) **Hold the #30 escalation rung — decided at cycle 5, later cycles execute it.** — Cycle 4 sent the first `high` that told the owner setup was still owed (12:46 PT). **Cycles 6 (~07:38Z ≈ 00:38 PT) and 7 (~13:38Z ≈ 06:38 PT) send `low` regardless** — *cycle 6 sent `low` (`msg_acdced06`); cycle 7 sent `low` as committed, without re-arguing the rung* — they land overnight/early for the owner. **If all four #30 objects are still absent at cycle 8 (~2026-09-19T19:38Z ≈ 12:38 PT), that cycle's notify goes at `high`**, a full 24 h after the first, in the owner's working afternoon, and is about #30 alone. **After cycle 8, stop escalating on a timer** — drop to one `low` mention per day, on the same reasoning that retired the #17 rung: an informed owner who has not acted is choosing, and repetition is not new information. Do not re-argue this rung each cycle. **✅ CYCLE 8 FIRED IT AND THE RUNG IS NOW RETIRED** — all four objects verified absent at 19:38Z, one `high` notify sent about #30 alone. **From cycle 9 on: no timed escalation on #30, one `low` mention per day.** If a later cycle feels the urge to escalate again, the answer is already written down: an informed owner who has not acted is choosing, and repetition is not new information.

- [ ] (2026-09-19, cycle 8) **NEVER add `needs-human` to #22 or #30 — the §4h sweep is aimed at exactly those two.** — The skill's §4h blocked-queue sweep enumerates `label:blocked sort:created-asc -label:needs-human` and `label:waiting sort:created-asc -label:needs-human`. Run at cycle 8 those return **exactly {#22, #30}** — the two issues that must never be parked with `needs-human`: it would suppress #22's own §4d cycle-10 closure sweep, and it would switch off this routine's resume check, which enumerates open issues labelled `waiting`. §4h's escalate branch fires on *"aged past `closure_aging_window_hours` with the blocker unchanged and no path you can verify"*; both are past 48 h and neither has a `Blocked by #N` dependency, so a run matching on age alone lands there. **Both are branch (b) — "still legitimately blocked, leave it" — and these are the verifiable paths, so no cycle has to re-derive them:** #22 → the §4d aging sweep at **cycle 10 (~2026-09-20T07:38Z)**, objection window shuts 2026-09-20T01:42Z; #30 → the owner's **`DONE private-inbox`**, checked by four objective existence checks every cycle. Do **not** solve this by inventing a different label to dodge the sweep: every label in this taxonomy is load-bearing for some other actor's search, and label-dodging is how #21, #22 and #30 each became traps. **Cycles 9, 10 and 11 all re-ran the enumeration: still exactly {#22, #30}, neither carrying `needs-human` — no regression, FOURTH consecutive clean check.** Both remain branch (b), and both paths are named: #22 → **dev-team step-0 adjudication on resume** (revised at cycle 9 — the cycle-10 sweep is no longer the path, see the rewritten sweep task), #30 → the owner's `DONE private-inbox`. **Standing, every cycle. If a future cycle finds `needs-human` on #22 or #30, that is a regression** — remove it, and check whether the resume check or the closure sweep stopped silently in the meantime.

- [ ] (2026-09-18, cycle 3) Never again report "no workable issue" without the evidence. — Cycles 1–2 justified the disabled routine as an empty backlog; it was not empty. Done when: **standing** — every cycle that claims there is no agent work cites the `gh issue list --json number,labels,assignees` result that shows it, rather than inferring it from the routine being off. Cycles 4–9: complied — the list is quoted in each cycle's digest open-count accounting.

## Done

- [x] (2026-09-18, cycle 1 → **DONE cycle 10**) Split #17 into the takeover mitigation and the out-of-v1 domain switch. — **Cleared at cycle 10, six cycles after it was queued, five of those on a rationale that was never true.** Executed by the zero-delta route: **#17 retitled** to *"Security: close the redlandswifiproject.com takeover window (verify the domain, or drop the DNS records)"*, with an **additive banner** (`issuecomment-5748460508`) marking which paragraphs of the original body are now out of scope and the original text preserved verbatim. **No second issue filed** — the domain *switch* is Out of Scope (v1) in the ratified PRD and the owner answered `not-yet`, so it is now tracked by no issue at all, deliberately. **Priority left at `p2`, not raised to p1**: `needs-human` makes the tier label inert on this issue, and raising it would be a silent re-escalation against an owner decision whose rung is retired. Original text kept for the record:
  > - [ ] (2026-09-18, cycle 1) Split #17 into the p1 takeover mitigation and the out-of-v1 domain switch. — Done when: either a separate issue exists covering only "verify the domain on the GitHub account **or** remove the apex `A` / `www` `CNAME` records", with #17 retitled to the switch alone; **or** the exposure is gone (a `_github-pages-challenge-patrickmroskam` TXT resolves, or the apex `A` records no longer point at 185.199.108-111.153) and #17 is back at p3. **Cycle 9 correction — this was never budget-blocked.** Cycles 2–8 all deferred it as *"§4e needs ≥ 1 realized close"*. True of the **original** route (file a second issue); **not** true of the route cycle 4 actually chose — **retitling #17 to the mitigation alone files nothing, so net issue delta is 0** and §4e (which budgets *filing*) never applied. The deferral survived on an expired rationale. It matters now because the #22 sweep **will not** produce a close (rewritten above), so a cycle-10 run re-reading "blocked on budget" would defer it a seventh time for a reason that was never true. **It is unblocked — do it at cycle 10**, with the full rebuild. **Cycle 4 narrows the goal:** the owner's `not-yet` confirms the ratified PRD's Out-of-Scope line, so the domain *switch* needs no issue in v1 at all — the split can be satisfied by retitling #17 to the mitigation alone, at zero net issue delta, rather than by opening a second issue. Deferred to the same cycle regardless, to keep the retitle in one change with the sweep.

- [x] (2026-09-18, cycle 2 → retired cycle 4) Hold the #17 escalation rung. — **Retired unfired.** The rung's condition was "exposure still live **and** the ask still unanswered at cycle 4"; the ask came back `not-yet`, so the condition was never met. Cycle 4 re-aimed the escalation at #30 instead, on its own merits. Original text kept for the record:
  > - [ ] (2026-09-18, cycle 2) Watch #17's ask `msg_e812f339-e53b-46d4-bb67-ce8e5f6edced` and hold the escalation rung. — **Do not re-ask while it is open.** The gate is the DNS, not the reply: re-resolve the challenge TXT and the apex `A` records every cycle. Done when: either the exposure is closed (TXT resolves → drop #17 to p3 and retitle to the switch alone; or apex `A` leaves the GitHub range → close #17), or — if the exposure is still live *and* the ask still unanswered at **cycle 4 (~2026-09-18T19:38Z ≈ 12:38 PT)**, a full waking day with the ask in front of the owner — that cycle sends its notify at priority `high`. Cycle 3 (~06:38 PT) sends nothing new. **The rung was decided at cycle 2; later cycles execute it, they do not re-argue it.** Cycle 3: executed as written — gate re-resolved (still exposed), ask re-checked with `ask await` (still open), no re-ask, one `low` notify. **Cycle 4 fires the `high` rung, and keeps that message about the takeover window alone.**

- [x] (2026-09-18, cycle 3) Re-run both objective gates and hold the committed rung. — #30: repo / `ingest` environment / secrets all still absent (and variables 0). #17: challenge TXT still absent, apex `A` still in the Pages range, apex still 404, `pages.cname` still null; ask `msg_e812f339` re-checked → still open, not re-asked. One `low` notify sent; escalation left for cycle 4 as committed.

- [x] (2026-09-18, cycle 3) Correct the record on why the dev-team routine is off. — It is the constitution's step 4, not an empty backlog. Fixed in the state digest and the readiness report, logged as a reversal in the decision log, and turned into the standing evidence rule above.

- [x] (2026-09-18, cycle 2) Send the #17 ask that had never been sent. — Cycle 1 diagnosed the deadlock; cycle 2 cleared it: wrote `docs/setup/domain-takeover.md` (`f7368e0`), sent ask `msg_e812f339-…`, recorded it on #17, resume key `DONE domain-takeover`.

- [x] (2026-09-18, cycle 2) Chase #21 (p3, owner-only GitHub security settings). — Closed by recording on the issue that its steps have never reached the owner through the durable channel and that it is deliberately deferred behind the p1/p2 owner queue, with the unblock condition named. Rejected sending an ask of its own: a third, p3 ask competing with a live p1 security item lowers the odds of both.
- [x] (2026-09-18, cycle 1) Bootstrap the continuity artifacts (digest, decision log, PO tasks, objection ledger).
- [x] (2026-09-18, cycle 1) Clear `needs-triage` from #22, as the 2026-09-17 09:09Z comment requested.
